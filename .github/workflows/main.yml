name: Hello World

on: push

jobs: 
  build:
   runs-on: ubuntu-latest

   steps: 
    - uses: actions/checkout@v4
    
    - name: Run a sample Workflow
      run: echo "Hi! this workflow runs sucessfully"
